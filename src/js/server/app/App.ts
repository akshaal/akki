// Don't modify this class unless you don't care about merge conflict with upstream / master branch.
// Customization is supposed to be done using ./index.ts or CustomAppModule.ts.

import { NodeJsApp } from 'server/akjs/nodejs-app/NodeJsApp';

export class App extends NodeJsApp {}
